
Created controllers for login, register, and logout:
- Implemented login controller to authenticate users and issue tokens.
- Built register controller to handle new user sign-up and password encryption.
- Developed logout controller to clear authentication tokens and end user sessions.

Created auth route:
- Defined routes for login, register, and logout, linking them to their respective controllers.

Registered auth route in app.js:
- Imported the auth route and added it to the Express app to handle authentication requests.